import 'package:flutter/material.dart';

class AppText {
  final headerText = TextStyle(color: Colors.blue, fontSize: 25);

  // Shipment text
  final shipmentLittleText = TextStyle(color: Colors.white, fontSize: 10);
  final location = TextStyle(color: Colors.white, fontSize: 15);
  final shipmentTitle = TextStyle(color: Colors.white, fontSize: 25);

  AppText();
}
